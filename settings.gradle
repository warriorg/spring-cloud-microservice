rootProject.name = 'microservice-springcloud-sample'

include 'shop-account'
include 'shop-gateway'
include 'shop-infrastructure'


file('shop-coupon').eachDir { dir ->
    include dir.name
    project(":${dir.name}").projectDir = dir
}
